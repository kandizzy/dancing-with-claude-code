import type { LevelDefinition } from './types'

export const level3: LevelDefinition = {
  id: 3,
  shape: 'arc',
  title: 'Write a directive, not a chat',
  capability: 'Compose a directive with three explicit parts (scope, target, single change) instead of an open-ended chat',
}
