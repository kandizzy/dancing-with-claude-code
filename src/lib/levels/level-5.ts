import type { LevelDefinition } from './types'

export const level5: LevelDefinition = {
  id: 5,
  shape: 'composite',
  title: 'Scope to one segment',
  capability: 'Constrain a change to exactly one segment of your project — leaving everything else alone',
}
