import type { LevelDefinition } from './types'

export const level2: LevelDefinition = {
  id: 2,
  shape: 'triangle',
  title: 'Slash command discovery',
  capability: 'Invoke a prepared slash command instead of typing out a long prompt from scratch',
}
