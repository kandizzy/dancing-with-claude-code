import type { LevelDefinition } from './types'

export const level4: LevelDefinition = {
  id: 4,
  shape: 'square',
  title: 'Review before you accept',
  capability: 'Read what Claude actually did, in a diff, before moving on',
}
